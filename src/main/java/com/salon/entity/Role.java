package com.salon.entity;

public enum Role {
    CLIENT,    // клиент — видит свои записи и абонемент
    EMPLOYEE,  // сотрудник — видит своё расписание
    ADMIN,     // администратор — управляет записями и клиентами
    OWNER      // руководитель — полный доступ + аналитика
}
